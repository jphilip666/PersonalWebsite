name: Status Check
run-name: ${{ github.actor }} is running some tests out the pull request 🚀
on:
  push:
    branches-ignore:
      - main
jobs:
  Check-Status:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event. PULL REQUEST"
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
