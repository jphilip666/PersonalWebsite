name: Deploy Frontend Image
run-name: ${{ github.actor }} is deploying a Frontend Docker image 🚀
permissions:
  contents: write   # allows git push / tag creation
  packages: write
  actions: read
on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'  
jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      frontend_version: ${{ steps.frontend_version.outputs.version }}
    steps:
      # Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 24

      # Install dev dependencies
      - name: Install dev deps
        working-directory: ./frontend
        run: npm install --only=dev  

      # Run semantic-release to determine the new version
      - name: Run semantic-release
        id: semantic_release        
        working-directory: ./frontend
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release
          
      # Get the frontend version from the latest tag    
      - name: Get frontend version
        id: frontend_version
        if: ${{ steps.semantic_release.outcome == 'success' }}
        working-directory: ./frontend
        run: |
          if [ -f version.env ]; then
            cat version.env
            VERSION_RAW=$(grep VERSION version.env | cut -d '=' -f2)
            VERSION=${VERSION_RAW#frontend-v}
            echo "version=$VERSION" >> $GITHUB_OUTPUT
          fi

  build: 
    needs: release
    if: ${{ needs.release.outputs.frontend_version != '' }}
    runs-on: ubuntu-latest       
    steps:
      # Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Log in to Docker Hub  
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}  

      # Get the Docker Buildx action set up  
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Build and push the Frontend Docker image    
      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/personal-website-frontend:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/personal-website-frontend:${{ needs.release.outputs.frontend_version }}

      # Log out from Docker Hub
      - name: Log out from Docker Hub
        run: docker logout    
